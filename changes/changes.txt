2026-04-28 — Initial implementation (Next.js QR PNG generator)

- Scaffolded Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS v4, ESLint.
- Added dependency: qr-code-styling (canvas mode, PNG export).
- Implemented lib/qr-payload.ts: URL/text/Wi‑Fi/image payload helpers and validation caps.
- Implemented lib/qr-instance.ts: shared QR options (size 512, error correction, styling).
- Implemented lib/compose-qr-png.ts: composite QR PNG over user background (cover fit); download helper.
- Implemented components/qr-workspace.tsx: client UI with mode tabs, separate appearance uploads, live preview, Download PNG.
- Updated app/page.tsx and app/layout.tsx: landing copy and metadata; Geist font on body.
- Added prompt.txt (restructured prompt log) and this changelog per project convention.

2026-04-29 — Copy update for image payload guidance

- Updated `lib/qr-payload.ts` image-mode empty-input validation message:
  "Paste an image URL or upload a small image file."
  -> "Paste an image URL or upload a ~2KB Image file"
- Logged prompt update in `prompt.txt` per project convention.

2026-04-30 — Refactor qr-workspace into smaller components

- Added `components/qr-workspace/content-section.tsx` for QR content modes and mode-specific form controls.
- Added `components/qr-workspace/appearance-section.tsx` for decorative upload controls (background/logo).
- Added `components/qr-workspace/preview-panel.tsx` for preview rendering and download CTA.
- Updated `components/qr-workspace.tsx` to keep state, validation helpers, QR sync/composition, and delegate UI rendering to the new components.
- Extracted image-embed upload handling into a dedicated callback for clearer parent orchestration.
- Logged the refactor request in `prompt.txt` per project convention.

2026-04-30 — System-dependent dark/light mode (no toggle)

- Added `components/system-theme-sync.tsx` to listen for `prefers-color-scheme` changes and keep `<html>` `dark` class, `data-theme`, and `color-scheme` in sync.
- Updated `app/layout.tsx` with a `beforeInteractive` inline script to set theme at first paint and mounted `SystemThemeSync` for runtime updates.
- Kept behavior toggle-free: theme is fully driven by OS settings.
- Logged the prompt in `prompt.txt` per project convention.

2026-04-30 — Hydration warning suppression for theme init

- Updated `app/layout.tsx` to add `suppressHydrationWarning` on the root `<html>` element.
- This suppresses expected attribute mismatch warnings caused by intentional pre-hydration updates to `data-theme` and `color-scheme`.
- Logged the follow-up prompt in `prompt.txt` per project convention.
